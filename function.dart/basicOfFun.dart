//reuseable code define into the fun.& call it whenever its need.
void main() {
  String greeting() //fun with no paramtr and return value
  {
    return "welcome to the Dart";
  }

  print(greeting());
}
